{
  "name": "vidmix",
  "version": "0.1.0",
  "description": "VidMix Studio - browser-based video/audio mixing editor, with a thin Electron shell for a native Mac build. All media processing runs client-side via ffmpeg.wasm.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . VidMix --platform=darwin --arch=x64",
    "test": "node --test test/"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
