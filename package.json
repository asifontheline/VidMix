{
  "name": "vidmix",
  "version": "0.1.0",
  "description": "Local video/audio mixing desktop app (Electron + ffmpeg)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . VidMix --platform=darwin --arch=x64"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.2"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
